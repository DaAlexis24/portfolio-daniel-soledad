---
interface Props {
    title: string,
    format: string,
    image: string,
}

const {title, format, image} = Astro.props
---
<article>
    <div>
        <p>{format}</p>
    </div>
    <img src={image} alt={title} title={title} />
    <h4>{title}</h4>
</article>